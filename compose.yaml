services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=teachflow_db'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'