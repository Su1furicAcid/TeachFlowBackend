# Getting Started

Before starting the project, remember setting the environment variable `DOCKERHUB_USERNAME` in `.env`.

Then, 

```bash
docker-compose up -d
```

The api document is released on `localhost:8080//swagger-ui/index.html`
